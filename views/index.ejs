<%- include('partials/header') -%>
<!--    <h1 class="text-4xl">A global platform with our</h1>
    <h1 class="text-4xl">custom solutions</h1>
    -->
    
    <% if (locals.isAuthenticated) { %>
        <p>Welcome back, <%= user.nickname %>.</p>
    <% } else { %>
        <p>If you already have an account, please sign in. <a href="/login" class="underline">Login</a>.</p>
        <p>If you do not have an account, please sign up. <a href="/register" class="underline">Register</a>.</p>
    
    <% } %>
    
<%- include('partials/footer') -%>
